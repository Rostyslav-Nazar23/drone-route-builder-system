# Інструкція з тестування алгоритмів

## Як перевірити коректність роботи алгоритмів

### 1. Запуск автоматичних тестів

```bash
# Запустіть всі тести
python run_tests.py

# Або через unittest
python -m unittest discover tests
```

### 2. Візуальна перевірка на карті

#### Що перевіряти:

1. **Стартова точка (START) 🏁**
   - Має бути **зелений** маркер з іконкою "play"
   - Має бути зелений коло навколо
   - Має відповідати депо (depot)

2. **Цільові точки (TARGETS) 🎯**
   - Мають бути **червоні** маркери з іконкою "flag"
   - Мають червоні кола навколо
   - Мають відповідати заданим цільовим точкам

3. **Фінішна точка (FINISH) 🏁**
   - Має бути **червоний** маркер з іконкою "stop"
   - Має бути червоний коло навколо
   - Має бути останній waypoint маршруту

4. **Маршрут**
   - Лінія маршруту має бути **синя/фіолетова/помаранчева** (не cyan)
   - Має з'єднувати всі waypoints
   - Має бути плавним (без різких зигзагів)

5. **Стрілки вітру**
   - Мають бути **cyan** (блакитний) колір
   - Мають показувати напрямок вітру
   - Мають відрізнятися від маршруту

### 3. Перевірка різних алгоритмів

#### A* (стандартний)
- Шлях має бути оптимальним
- Має проходити через всі цільові точки
- Перевірте метрики (відстань, енергія)

#### Theta* (any-angle)
- Шлях має бути **більш плавним** ніж A*
- Може мати менше waypoints
- Може бути коротшим за відстанню

#### D* (dynamic replanning)
- Має знаходити шлях як A*
- Підтримує перепланування при змінах

### 4. Перевірка валідації

Перевірте, що валідація показує:
- ✅ **Зелений** статус, якщо маршрут валідний
- ❌ **Червоний** статус з переліком порушень, якщо є проблеми

### 5. Типові проблеми та рішення

#### Проблема: Маршрут не проходить через цільові точки
**Причина:** Алгоритм не знайшов шлях або граф не пов'язаний
**Рішення:** 
- Перевірте, чи всі точки в межах діапазону дрона
- Спробуйте збільшити `max_range` дрона
- Спробуйте використати Grid Graph замість Waypoint Graph

#### Проблема: Маршрут має різкі повороти
**Причина:** Використовується A* з дискретним графом
**Рішення:** 
- Спробуйте Theta* для більш плавних маршрутів
- Зменште resolution grid graph

#### Проблема: Маршрут занадто довгий
**Причина:** Не оптимальний алгоритм або не враховані обмеження
**Рішення:**
- Спробуйте Genetic Algorithm оптимізацію
- Перевірте, чи правильно налаштовані обмеження

### 6. Порівняння алгоритмів

Створіть одну місію і спробуйте різні алгоритми:

```python
# У Streamlit UI:
# 1. Створіть місію з кількома цільовими точками
# 2. Плануйте з A* - збережіть метрики
# 3. Плануйте з Theta* - порівняйте метрики
# 4. Плануйте з D* - порівняйте метрики
```

**Що порівнювати:**
- Загальна відстань
- Енергоспоживання
- Кількість waypoints
- Плавність маршруту (візуально)

### 7. Тестові сценарії

#### Сценарій 1: Простий маршрут
- 1 дрон
- 2-3 цільові точки
- Без no-fly зон
- Очікуваний результат: Прямий маршрут через точки

#### Сценарій 2: З обходом перешкод
- 1 дрон
- 3-5 цільових точок
- 1-2 no-fly зони
- Очікуваний результат: Маршрут обходить зони

#### Сценарій 3: Мультидрон
- 2-3 дрони
- 5-10 цільових точок
- З VRP
- Очікуваний результат: Оптимальний розподіл точок

### 8. Перевірка метрик

Після планування перевірте:
- **Distance**: Чи розумна відстань?
- **Time**: Чи вкладається в обмеження?
- **Energy**: Чи не перевищує батарею?
- **Waypoints**: Чи достатньо детальний маршрут?

### 9. Діагностика проблем

Якщо маршрут виглядає дивно:

1. **Перевірте валідацію** - можуть бути порушення обмежень
2. **Перевірте метрики** - можливо маршрут занадто довгий
3. **Спробуйте інший алгоритм** - Theta* може дати кращий результат
4. **Змініть параметри графа** - Grid vs Waypoint graph
5. **Перевірте координати** - чи правильно введені точки

### 10. Корисні команди

```bash
# Запуск тестів
python run_tests.py

# Тестування базової функціональності
python test_system.py

# Запуск Streamlit з детальними логами
streamlit run app/streamlit_app.py --logger.level=debug
```

